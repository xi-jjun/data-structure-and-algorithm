/**
 * leetcode
 * problem link : https://leetcode.com/problems/wildcard-matching/?envType=problem-list-v2&envId=greedy
 * title : 44. Wildcard Matching
 * type : greedy
 * difficulty : Hard
 */

#include <iostream>
#include <map>
#include <string>

using namespace std;

class Solution {
public:
    bool isMatch(string s, string p) {

    }
};

// Main
int main() {
    Solution sol;
    map<string, string> examples;
    examples.insert({"abcdef", "ab"});
    examples.insert({"abcdef", ""});
    examples.insert({"abcdef", ""});
    examples.insert({"abcdef", ""});
    examples.insert({"abcdef", ""});
}
